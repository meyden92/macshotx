# Generated by XcodeGen — regenerate with `xcodegen generate`
*.xcodeproj

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.hmap
DerivedData/
build/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
dist/

# macOS
.DS_Store

# Editor
.idea/

.scratch/

# Personal (uncommitted) skill rules
.claude/skill-context/**/*.local.md

# Time tracking stays on the machine that recorded it — the plugin books to its
# own data dir (local mode). The marker that used to force a committed ledger
# is deliberately absent; re-adding it would put working hours back in the repo.
.claude/timetracking/
.timetracking.json